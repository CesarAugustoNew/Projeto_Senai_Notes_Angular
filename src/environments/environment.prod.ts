// Este arquivo é sobrescrito automaticamente pelo script scripts/set-env.js
// antes do build de produção, usando a variável de ambiente API_URL (configurada
// na Vercel). O valor abaixo é só um placeholder para quando ninguém rodar o
// script (ex.: build local sem a variável definida).
export const environment = {
  production: true,
  apiUrl: 'http://localhost:8080',
};
